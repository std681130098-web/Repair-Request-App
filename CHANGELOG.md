# Changelog

บันทึกการเปลี่ยนแปลงของระบบแจ้งซ่อมออนไลน์ (รูปแบบ [Keep a Changelog](https://keepachangelog.com/))

## [Unreleased]
### เพิ่ม (Added)
- **โฉมใหม่ทั้งระบบ “ศูนย์งานซ่อม” (ธีมอบอุ่น terracotta + โหมดมืด)** — ออกแบบใหม่ทุกหน้า
  ตามต้นแบบ Claude Design: เปลี่ยนจาก navbar ด้านบนเป็น **เลย์เอาต์ sidebar + แถบบน**
  (ค้นหา, การแจ้งเตือน, ปุ่มแจ้งซ่อมใหม่), การ์ดมุมมน เงานุ่ม พื้นหลังลายเกรน,
  ป้ายสถานะแบบ pill, ไทม์ไลน์ความคืบหน้า, ชิปตัวกรองในหน้ารายการ และการ์ดงานแบบใหม่
  ครบทั้งหน้าหลัก/รายการ/รายละเอียด/ห้องแชท/แจ้งซ่อม/คอนโซลจัดการ/รายงาน
- **โหมดสว่าง/มืด** — ปุ่มสลับบนแถบบน จำค่าไว้ใน localStorage (ไม่กะพริบตอนโหลด) และ
  ปรับสีทั้งระบบรวมถึงคอมโพเนนต์ของ Bootstrap ให้เข้าธีม
- **ฟอนต์ IBM Plex Sans Thai (เก็บในโปรเจกต์)** — ดาวน์โหลดมาไว้ที่ `static/vendor/fonts/`
  เพื่อคงหลักการ offline-ready (ไม่พึ่ง Google Fonts CDN); ไอคอนประเภทงานอัตโนมัติตามชื่อประเภท
- **บันทึกความคืบหน้าอัตโนมัติในห้องแชท** — ทุกครั้งที่สถานะงานเปลี่ยน (มอบหมายช่าง / เริ่มซ่อม /
  แจ้งงานเสร็จ / ตรวจรับผ่าน / ตีกลับพร้อมเหตุผล) ระบบจะโพสต์ข้อความอัปเดตลงในเธรดสนทนาให้อัตโนมัติ
  แสดงเป็นเส้นเวลากลางห้อง — ผู้แจ้งเห็นความคืบหน้าของงานได้แบบเรียลไทม์ในที่เดียวกับการพูดคุย
- **แจ้งเตือนข้อความใหม่ (unread) + เข้าห้องแชทได้จากทุกที่** — ป้ายตัวเลขข้อความที่ยังไม่ได้อ่าน
  บนเมนูและในตารางรายการ (นับเฉพาะข้อความที่ตนมีสิทธิ์เห็นและไม่ใช่ของตัวเอง),
  มีปุ่มเข้าห้องสนทนาในทุกแถวของรายการ และเคลียร์ตัวเลขอัตโนมัติเมื่อเปิดอ่าน
- **UX แนบไฟล์ในห้องแชทที่ลื่นขึ้น** — ลากไฟล์มาวาง (drag & drop), วางรูปจากคลิปบอร์ด (Ctrl+V),
  แสดงชิปไฟล์ที่เลือกพร้อมปุ่มลบก่อนส่ง, กด Enter เพื่อส่ง (Shift+Enter ขึ้นบรรทัดใหม่),
  กล่องพิมพ์ยืดอัตโนมัติ และคลิกรูปเพื่อดูขนาดใหญ่ (lightbox) — ทั้งหมดยังส่งได้ตามปกติแม้ปิด JavaScript
- **ห้องสนทนาแยกหน้า + แนบไฟล์** — ย้ายการสนทนาเป็นปุ่ม "เปิดห้องสนทนา" บนหน้ารายละเอียด
  กดแล้วเปิดหน้าห้องแชทเต็ม (`/requests/<id>/chat/`) ในแท็บใหม่, แนบรูป/ไฟล์ได้หลายไฟล์
  (รูปพรีวิวในแชท · เอกสารเป็นลิงก์ดาวน์โหลด · จำกัดชนิดไฟล์และ ≤10 MB เพื่อความปลอดภัย),
  ส่งเฉพาะไฟล์โดยไม่มีข้อความก็ได้
- **เธรดสนทนาบนใบแจ้งซ่อม** — ผู้แจ้ง / ผู้ดูแล / ช่าง พูดคุยและติดตามความคืบหน้าในใบเดียวกัน,
  แสดงชื่อ+บทบาท+เวลาแบบแชท พร้อมตัวเลือก "หมายเหตุภายใน" ที่เห็นเฉพาะผู้ดูแลและช่าง (ผู้แจ้งไม่เห็น)
- **คอนโซลจัดการระบบสำหรับผู้ดูแล (`/manage/`)** — หน้า dashboard ดีไซน์เดียวกับเว็บ (Bootstrap + Sarabun)
  แทนการพึ่ง Django admin: สรุปสถิติ (KPI), คิว "รอมอบหมายช่าง" / "รอตรวจรับ" ที่กดจัดการได้ทันที,
  ตารางภาระงานช่าง (กำลังทำ/เสร็จแล้ว), เพิ่มประเภทงานซ่อมได้ในหน้าเดียว และสรุปจำนวนผู้ใช้แต่ละบทบาท
- **ช่างแนบรูปตอนแจ้งงานเสร็จ** — ช่างถ่ายรูปงานที่ซ่อมเสร็จแนบพร้อมบันทึกการซ่อม (สูงสุด 5 รูป รูปละ 5 MB),
  แสดงแยกเป็นแกลเลอรี "รูปงานที่ซ่อมเสร็จ" ในการ์ดการมอบหมายงาน (แยกจากรูปที่ผู้แจ้งถ่าย)
- **แนบรูปภาพในใบแจ้งซ่อม** — ผู้แจ้งถ่ายรูปจุดที่ต้องซ่อมได้หลายรูป (สูงสุด 5 รูป รูปละ 5 MB),
  แสดงเป็นแกลเลอรีในหน้ารายละเอียด และลบรูปได้ระหว่างที่ใบยังแก้ไขได้
- ดึงไฟล์ Bootstrap, Bootstrap Icons, ฟอนต์ Sarabun มาเก็บในโปรเจกต์ (`static/vendor/`) — หน้าเว็บทำงานได้แม้ไม่มีอินเทอร์เน็ต
- คู่มือการทำงานเป็นทีมด้วย Git/GitHub (`docs/TEAM-GIT-WORKFLOW.md`) + แม่แบบ Pull Request
- `.gitattributes` ป้องกันไฟล์ฟอนต์เสียตอน checkout ข้ามระบบปฏิบัติการ
- ชุดทดสอบเพิ่มขึ้นเป็น 59 เคส (ครอบคลุมการอัปโหลด/ลบรูป, เธรดสนทนา, แนบไฟล์, บันทึกความคืบหน้าอัตโนมัติ และตัวนับข้อความที่ยังไม่ได้อ่าน)

### แก้ไข (Fixed)
- ปุ่ม "ตีกลับ" ของผู้ดูแลไม่แสดงบนใบที่ถูกตีกลับไปแล้ว (เดิมกดแล้วขึ้น error)
- ป้ายสถิติหน้าแดชบอร์ดผู้แจ้งเปลี่ยนเป็น "ที่ยังไม่เสร็จ" ให้ตรงกับค่าที่นับจริง

## [1.0.0] - 2026-07-22
เวอร์ชันแรกที่ใช้งานได้จริง
### เพิ่ม (Added)
- โครงสร้างฐานข้อมูล 5 ตาราง (User, Category, RepairRequest, Assignment, Rating)
- ระบบสมาชิก 3 บทบาท พร้อมควบคุมสิทธิ์ตามบทบาท (role-based access control)
- กระบวนการแจ้งซ่อมครบวงจร: แจ้ง → ตรวจสอบ/อนุมัติ → มอบหมาย → ซ่อม → ตรวจรับ → ประเมิน
- แดชบอร์ดแยกตามบทบาท (ผู้แจ้ง / ผู้ดูแล / ช่าง)
- หน้ารายงานสรุป (ตามสถานะ, ประเภทงาน, ภาระงานช่าง, คะแนนเฉลี่ย)
- UI ภาษาไทยด้วย Bootstrap 5 + ฟอนต์ Sarabun
- คำสั่ง `seed_data` สร้างข้อมูลตัวอย่างและบัญชีทดสอบ
- ชุดทดสอบอัตโนมัติ 18 เคส
- GitHub Actions CI (ตรวจสอบและทดสอบอัตโนมัติ)

<!--
รูปแบบการบันทึกเวอร์ชันถัดไป — คัดลอกไปใช้เมื่ออัพเดท Patch:

## [1.1.0] - YYYY-MM-DD
### Added        (เพิ่มฟีเจอร์ใหม่)
### Changed      (แก้ไข/ปรับปรุง)
### Fixed        (แก้บั๊ก)
-->
